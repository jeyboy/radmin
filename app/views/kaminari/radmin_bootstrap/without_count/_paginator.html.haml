- pagination_class ||= ''
- nav_class ||= ''
= paginator.render do
  %nav{:class => nav_class}
    %ul{:class => "pagination #{pagination_class}"}
      = prev_page_tag unless current_page.first?
      = next_page_tag
